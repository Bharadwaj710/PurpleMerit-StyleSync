{
  "name": "stylesync-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "postinstall": "npx puppeteer browsers install chrome",
    "build": "node -e \"console.log('backend build step not required yet')\"",
    "lint": "node -e \"console.log('backend lint not configured yet')\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.8",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "node-vibrant": "^4.0.4",
    "puppeteer": "^24.6.1"
  }
}
